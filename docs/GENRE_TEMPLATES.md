# 类型小说玩法模板

本文档记录不同类型小说对应的 RPG 数值、场景交互和玩法循环。Skill 后续应根据原作类型自动选择模板。

## 通用规则

- 类型模板只提供玩法骨架，不替代原作风格。
- 可见数值最多 5 个。
- 隐藏数值可以用于条件、routes、结局，但不要在普通 UI 中暴露。
- 每个选择必须先有文本回响，再有数值反馈。
- 数值变化必须能被剧情解释。
- **选择重量**：关键分歧选择（`weight=critical`）每章不超过 1 个，`weight=branch` 的选择每章不超过 3 个。critical 选择应当影响路线走向或结局条件；branch 选择提供次要分歧但不应拆分主线。
- **渐进探索**：每个节点最多配置 1 个 `surface` + 1 个 `deep` + 1 个 `ultimate` 交互。surface 为表层行为（一看即得），deep 为深入行为（需要观察或思考），ultimate 为终极发现（需要前置条件或多次交互积累）。
- **里程碑**：每个类型必须至少包含 1 个 `large` 里程碑（核心成长节点），用于标志玩家在剧情中的关键性进展。里程碑分为 small / medium / large 三级，均可配置 VFX 特效。
- **三层后果**：`weight=critical` 的选择必须配置至少 1 个 `delayedChanges`（延迟后果），即选择的真正影响不会立即显现，而是在后续章节中以出人意料的方式爆发。
- **结局设计**：每个类型至少 4 个结局（1 true + 1 dark + 1 neutral + 1 hidden），结局总数不超过 8 个。结局由数值条件、flag 和关键选择的综合结果判定。

## 修仙

### 核心体验

境界成长、灵力资源、功法选择、机缘探索、因果/道心、突破风险。

### 核心循环

```text
入门/当前困境
  -> 修炼/悟道
  -> 资源选择
  -> 外出历练
  -> 人情因果
  -> 突破/失败
  -> 新境界或新危机
```

### 推荐变量

| 变量 | 含义 | 展示建议 |
| --- | --- | --- |
| `realm` | 境界 | 可见 |
| `cultivation` | 修为进度 | 可见 |
| `qi` | 灵力 | 可见 |
| `daoHeart` | 道心 | 可见 |
| `comprehension` | 悟性 | 可见或隐藏 |
| `karma` | 因果/业力 | 隐藏 |
| `tribulationRisk` | 突破风险 | 隐藏 |
| `sectReputation` | 宗门声望 | 可见 |
| `masterTrust` | 师父信任 | 可见 |

### 推荐交互

- 打坐修炼。
- 内视丹田。
- 参悟功法。
- 探查灵气。
- 炼丹。
- 试剑。
- 接宗门任务。
- 突破境界。

### 推荐里程碑

#### small

- 首次修炼：完成第一次打坐冥想，感应灵气入体。
- 首次获胜：在切磋或外门小比中首次战胜对手。
- 获得法宝：第一次获得法宝或灵器。
- 结交道友：与一位重要同辈建立友谊或同盟关系。

#### medium

- 突破小境界：在同一大境界内完成小阶段突破（如炼气三层到五层）。
- 领悟功法：深入理解一门功法的奥义，解锁新的技能或用法。
- 宗门大比晋级：在宗门考核或大比中成功晋级，获得更高权限或资源。

#### large

- 大境界突破：完成跨境界突破（如筑基成功、金丹凝结、元婴出窍），修为发生质变。
- 飞升：渡过天劫，离开当前世界位面。

#### VFX

- large 里程碑触发 `breakthrough` 特效：灵气粒子从角色周身上升飘散，屏幕边缘泛光，配合音效营造超脱之感。

### 推荐结局

| 结局 | 名称 | 触发条件 |
| --- | --- | --- |
| true | 飞升成仙 | 高道心 + 高修为 + 突破成功 |
| dark | 堕入魔道 | 低道心 + 杀戮过多 |
| neutral | 凡尘情缘 | 放弃修仙，选择红尘 |
| noble | 以身殉道 | 为保护宗门或道侣牺牲（高因果 + 关键选择） |
| hidden | 逆天改命 | 满足特殊条件 + 古道法 + 特殊机缘（隐藏数值达阈值） |
| romance | 仙侣双修 | 道心 + 感情双满 + 双方存活 |

### 渐进探索示例

以"修炼室"节点为例：

- **surface**：打坐修炼。玩家进行基础打坐操作，获得少量修为进度，感受灵气流动。无前置条件。
- **deep**：内视丹田。选择深入冥想，查看丹田内灵气分布状态，发现经脉中的细微阻塞或异常灵气波动。需至少完成 1 次 surface 交互。
- **ultimate**：发现功法秘密。通过深度内视，意外触碰到功法传承中的隐藏记忆碎片，揭示功法的真实来历或隐藏的修炼路径。需完成 deep 交互 + 悟性达到阈值。

### 模板要求

- 每章至少 1 个修炼或悟道互动。
- 每章至少 1 个资源/法宝/丹药选择。
- 每章至少 1 个道心或因果选择。
- 至少 1 个突破 routes 节点。
- 每章至少 1 个 `weight=critical` 选择（可跨章共享，即连续章节可共享同一个 critical 选择的后果阶段）。
- 每章至少 2 个 `surface` 交互 + 可选 `deep` / `ultimate`。
- 每个 Demo 至少包含 1 个 `large` 里程碑 + 2 个 `medium` + 3 个 `small`。
- 每个 Demo 结局总数 4-8 个。

## 无限恐怖 / 副本生存

### 核心体验

任务压力、团队信任、资源消耗、恐惧/理智、情报收集、支线奖励、回归结算。

### 核心循环

```text
主神空间
  -> 任务发布
  -> 进入恐怖世界
  -> 搜索情报
  -> 团队分歧
  -> 危机爆发
  -> 支线奖励/逃生抉择
  -> 回归结算
  -> 强化兑换
```

### 推荐变量

| 变量 | 含义 | 展示建议 |
| --- | --- | --- |
| `hp` | 生命 | 可见 |
| `sanity` | 理智 | 可见 |
| `points` | 奖励点 | 可见 |
| `teamTrust` | 团队信任 | 可见 |
| `danger` | 区域危险度 | 可见 |
| `clue` | 情报完成度 | 可见 |
| `timeLeft` | 剩余时间 | 可见 |
| `infection` | 感染/诅咒 | 隐藏或可见 |

### 推荐交互

- 搜索房间。
- 观察怪物规律。
- 检查尸体。
- 分配队伍。
- 使用道具。
- 救或放弃队友。
- 冒险刷支线。
- 回主神空间兑换强化。

### 推荐里程碑

#### small

- 首次存活：在第一个恐怖场景中活过第一夜或第一波攻击。
- 获得关键道具：找到对当前副本或后续副本有重要影响的道具。
- 队友信任提升：通过关键行动获得至少一名核心队友的信任。

#### medium

- 完成支线任务：在不影响主线目标的前提下完成额外奖励任务。
- 单副本全队存活：整个副本过程中无人死亡。
- 发现Boss弱点：通过情报收集找到Boss的致命弱点或通关捷径。

#### large

- 存活归来：完成副本回归主神空间，进行结算并获得奖励。
- S级评价：以最高评价完成副本，获得超额奖励和稀有兑换资格。

#### VFX

- large 里程碑触发 `tearIn` 特效：画面从中心向外撕裂，现实世界透过裂缝显现，营造从恐怖世界"撕回"主神空间的视觉冲击。

### 推荐结局

| 结局 | 名称 | 触发条件 |
| --- | --- | --- |
| true | 唯一幸存者 | 全队阵亡但主角存活 + 完成主线任务 + 高理智 |
| dark | 深渊凝视 | 主角存活但理智归零，精神崩溃 |
| noble | 牺牲救赎 | 主角死亡但全队存活 + 高团队信任 |
| hidden | 全员存活 | 所有队友存活 + S级评价 + 完成所有支线（极稀有） |

### 渐进探索示例

以"恐怖房间"节点为例：

- **surface**：环顾四周。玩家查看房间基本布局，了解可用的家具、门窗、光源。无前置条件。
- **deep**：检查暗门。仔细检查墙壁、地板和家具背后的隐蔽结构，发现隐藏的通道或密室入口。需完成 1 次 surface 交互。
- **ultimate**：发现安全屋密码。在暗门后的密室中找到前人留下的密码本或线索，获得通往安全屋的关键信息。需完成 deep 交互 + 情报完成度达到阈值。

### 模板要求

- 每个任务世界必须有明确目标。
- 必须有时间或危险度压力。
- 至少 3 个可调查场景。
- 至少 1 个团队信任变化。
- 至少 1 个支线奖励诱惑。
- 必须有回归结算或任务失败节点。
- 每章至少 1 个 `weight=critical` 选择（可跨章共享）。
- 每章至少 2 个 `surface` 交互 + 可选 `deep` / `ultimate`。
- 每个 Demo 至少包含 1 个 `large` 里程碑 + 2 个 `medium` + 3 个 `small`。
- 每个 Demo 结局总数 4-8 个。

## 悬疑推理

### 核心体验

线索收集、证词矛盾、嫌疑关系、推理完整度、错误推断成本。

### 推荐变量

| 变量 | 含义 | 展示建议 |
| --- | --- | --- |
| `deduction` | 推理完整度 | 可见 |
| `clueCount` | 线索数量 | 可见 |
| `pressure` | 调查压力 | 可见 |
| `suspectAlert` | 嫌疑人警觉 | 隐藏 |
| `mistake` | 错误推断次数 | 可见 |

### 推荐交互

- 调查现场。
- 询问人物。
- 追问。
- 比对证词。
- 提交推理。
- 指认证词矛盾。

### 推荐里程碑

#### small

- 发现第一条线索：在现场或对话中获取第一份关键证据。
- 嫌疑人确认：初步锁定至少一名嫌疑人。

#### medium

- 锁定凶手范围：通过排除和交叉验证，将嫌疑人缩小到较小范围。
- 排除错误嫌疑：推翻一个看似合理的假设，避免错误指控。

#### large

- 揭开真相：拼凑完整证据链，揭示案件全貌和凶手动机。

#### VFX

- large 里程碑触发 `typewriterFlash` 特效：画面变为黑白打字机效果，真相文字逐字浮现，伴随机械敲击声，营造真相大白的高潮氛围。

### 推荐结局

| 结局 | 名称 | 触发条件 |
| --- | --- | --- |
| true | 完美破案 | 推理完整度满 + 错误推断为0 + 揭示动机 |
| dark | 局中人 | 主角自己就是凶手（隐藏数值+关键选择触发） |
| neutral | 悬案 | 线索不足，真相未能完全揭示，案件悬而未决 |

### 渐进探索示例

以"犯罪现场"节点为例：

- **surface**：拍照记录。玩家对现场进行基础拍照和笔记记录，获取表层物证信息。无前置条件。
- **deep**：比对证词。将现场物证与已有证人证词进行交叉比对，发现证词中的矛盾或遗漏。需完成 1 次 surface 交互。
- **ultimate**：发现隐藏证据链。通过深度分析，找到被刻意隐藏的关键证据（如被清洗的血迹、伪造的痕迹），形成完整证据链。需完成 deep 交互 + 线索数量达到阈值。

### 模板要求

- 至少 3 条可收集线索。
- 至少 1 个需要 flag 或 clue 条件的推理选项。
- 错误推断应造成后果。
- 每章至少 1 个 `weight=critical` 选择（可跨章共享）。
- 每章至少 2 个 `surface` 交互 + 可选 `deep` / `ultimate`。
- 每个 Demo 至少包含 1 个 `large` 里程碑 + 2 个 `medium` + 3 个 `small`。
- 每个 Demo 结局总数 4-8 个。

## 末世生存

### 核心体验

食物、水、弹药、感染、庇护所安全度、队伍士气、昼夜循环。

### 推荐变量

| 变量 | 含义 | 展示建议 |
| --- | --- | --- |
| `hp` | 生命 | 可见 |
| `food` | 食物 | 可见 |
| `water` | 水 | 可见 |
| `ammo` | 弹药 | 可见 |
| `shelter` | 庇护所安全 | 可见 |
| `morale` | 队伍士气 | 可见 |
| `infection` | 感染 | 可见或隐藏 |
| `noise` | 噪音 | 隐藏 |

### 推荐交互

- 搜刮资源。
- 加固门窗。
- 分配物资。
- 侦察周边。
- 夜晚防守。
- 处理感染者。

### 推荐里程碑

#### small

- 首夜存活：在末世中成功度过第一个夜晚。
- 首次交易：与其他幸存者或势力完成第一次物资交换。

#### medium

- 建立庇护所：找到或建造一个相对安全的据点。
- 治愈感染者：成功找到药品或方法，治愈一名感染者。

#### large

- 找到安全区：抵达传说中的人类最后安全区域，获得相对稳定的生活环境。

#### VFX

- large 里程碑触发 `noiseStatic` 特效：画面出现强烈的噪点闪烁和信号干扰效果，象征从混乱的末世信号中接收到安全区的坐标信息。

### 推荐结局

| 结局 | 名称 | 触发条件 |
| --- | --- | --- |
| true | 新世界 | 抵达安全区 + 队伍存活过半 + 高士气 |
| dark | 覆灭 | 庇护所沦陷 + 主角存活但失去一切 |
| noble | 最后抵抗 | 主角牺牲但为同伴争取到撤离时间 + 高士气 |

### 渐进探索示例

以"废弃超市"节点为例：

- **surface**：搜刮食物。玩家在超市货架间快速搜找可用的食物和饮用水。无前置条件。
- **deep**：找到地下室。通过观察地面痕迹或建筑结构异常，发现通往地下储藏室的入口，可能藏有更多物资。需完成 1 次 surface 交互。
- **ultimate**：发现其他幸存者日记。在地下室深处找到前一批幸存者留下的日记本，记录了他们发现安全区线索的经过，或标注了危险区域的位置。需完成 deep 交互 + 侦察次数达到阈值。

### 模板要求

- 每个阶段至少 1 次资源消耗。
- 至少 1 个物资分配导致关系变化。
- 至少 1 个资源不足导致的替代路线。
- 每章至少 1 个 `weight=critical` 选择（可跨章共享）。
- 每章至少 2 个 `surface` 交互 + 可选 `deep` / `ultimate`。
- 每个 Demo 至少包含 1 个 `large` 里程碑 + 2 个 `medium` + 3 个 `small`。
- 每个 Demo 结局总数 4-8 个。

## 宫斗 / 权谋

### 核心体验

宠信、疑心、情报、把柄、派系关系、名声、利益交换。

### 推荐变量

| 变量 | 含义 | 展示建议 |
| --- | --- | --- |
| `favor` | 宠信 | 可见 |
| `suspicion` | 疑心 | 可见 |
| `reputation` | 名声 | 可见 |
| `silver` | 银钱 | 可见 |
| `intel` | 情报 | 可见 |
| `queenHostility` | 皇后敌意 | 可见 |
| `factionPower` | 派系力量 | 可见 |

### 推荐交互

- 送礼。
- 打探。
- 试探。
- 布置眼线。
- 交换利益。
- 保人或弃子。

### 推荐里程碑

#### small

- 首次获宠：在皇帝面前留下良好印象，获得初步关注。
- 首次拉拢盟友：成功说服或收买一名宫中人物为己用。

#### medium

- 晋升位分：正式获得更高位分，权限和资源随之增加。
- 瓦解对手势力：通过计谋或借刀杀人，削弱一个敌对派系。

#### large

- 执掌后宫：成为后宫中实际掌权者，掌控大局。
- 太后垂帘：以太后身份垂帘听政，影响力超越后宫。

#### VFX

- large 里程碑触发 `curtainOpen` 特效：华丽的帷幕从画面中央向两侧缓缓拉开，金光透入，象征权力格局的彻底改变。

### 推荐结局

| 结局 | 名称 | 触发条件 |
| --- | --- | --- |
| true | 母仪天下 | 高宠信 + 高派系力量 + 低疑心 |
| dark | 流放之路 | 高疑心 + 派系力量归零 + 重大失宠 |
| neutral | 离宫 | 主动或被动离开宫廷，选择平凡生活 |
| hidden | 太后垂帘 | 高宠信 + 高情报 + 特殊flag（皇帝驾崩或退位路线） |

### 渐进探索示例

以"御花园"节点为例：

- **surface**：散步赏花。玩家在御花园中散步，观察植物和建筑，偶遇其他宫中人物。无前置条件。
- **deep**：偷听宫人闲谈。在花园角落隐蔽处偷听宫女太监的私下谈话，获取宫廷内部消息或传闻。需完成 1 次 surface 交互。
- **ultimate**：发现密道入口。通过多次偷听和观察，注意到花园某处建筑的异常结构，发现通往密道的隐藏入口，可用于紧急逃生或秘密行动。需完成 deep 交互 + 情报达到阈值。

### 模板要求

- 至少 3 个势力或关键人物关系。
- 每个关键选择至少影响一个关系数值。
- 高收益选择必须增加疑心或树敌风险。
- 每章至少 1 个 `weight=critical` 选择（可跨章共享）。
- 每章至少 2 个 `surface` 交互 + 可选 `deep` / `ultimate`。
- 每个 Demo 至少包含 1 个 `large` 里程碑 + 2 个 `medium` + 3 个 `small`。
- 每个 Demo 结局总数 4-8 个。

## 普通文学 / 现实向

### 核心体验

保持原作气质，避免强行游戏化。只在必要时使用轻量状态。

### 推荐变量

- `trust`
- `distance`
- `memory`
- `resolve`
- `pressure`

### 模板要求

- 不强制使用 RPG 面板。
- 可只使用 `val` 和少量隐藏变量。
- 选择反馈以文本回响为主，数值反馈为辅。
- 每章至少 1 个 `weight=critical` 选择（可跨章共享），但 critical 选择的表现形式应自然融入叙事，不突兀。
- 每章至少 2 个 `surface` 交互 + 可选 `deep` / `ultimate`，但交互方式应以日常行为为主（如对话、观察、回忆）。
- 每个 Demo 至少包含 1 个 `large` 里程碑 + 2 个 `medium` + 3 个 `small`，里程碑的命名和表现形式应贴合现实题材（如"关系突破"、"重要决定"）。
- 每个 Demo 结局总数 4-8 个，结局侧重情感走向和人生选择而非数值判定。
